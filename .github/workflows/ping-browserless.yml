name: Ping Browserless Service

on:
  schedule:
    # Runs every 3 minutes
    - cron: '*/3 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Browserless
        run: |
          node -e "const https = require('https'); https.get('https://chrome-latest-3tox.onrender.com/?token=Imgonnacummmmm', (res) => { res.on('data', () => {}); res.on('end', () => { console.log('Status Code:', res.statusCode); }); }).on('error', (e) => { console.error('Error:', e.message); process.exit(1); });"
