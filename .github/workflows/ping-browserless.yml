name: Ping Browserless Service

on:
  schedule:
    # Runs every minute
    - cron: '* * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Browserless
        run: |
          curl -I -H "Authorization: Imgonnacummmmm" https://chrome-latest-3tox.onrender.com/
