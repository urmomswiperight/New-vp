name: Ping Browserless Service

on:
  schedule:
    # Runs every minute
    - cron: '* * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Browserless
        run: |
          curl -I --tlsv1.2 -H "Authorization: Imgonnacummmmm" https://production-sfo.chrome-latest-3tox.onrender.com/