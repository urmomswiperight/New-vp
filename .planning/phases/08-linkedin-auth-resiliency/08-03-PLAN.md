---
phase: 08-linkedin-auth-resiliency
plan: 03
type: complete
wave: 3
depends_on: [08-02]
files_modified: [tests/auth.spec.ts, TROUBLESHOOTING.md]
autonomous: false
requirements: [LNK-08-01, LNK-08-04, LNK-08-05]
user_setup: []

must_haves:
  truths:
    - "Integration tests verify storageState handles JSESSIONID correctly"
    - "Troubleshooting guide contains updated session extraction instructions"
    - "Manual verification confirms automation works with real storageState"
  artifacts:
    - path: "tests/auth.spec.ts"
      provides: "Integration testing for storageState injection"
    - path: "TROUBLESHOOTING.md"
      provides: "Updated instructions for users to resolve session issues"
---

<objective>
Perform final verification of the resilient LinkedIn automation system and document the new session management process for users.

Purpose: Ensure the end-to-end flow is stable and provide clear instructions for manual session recovery when health checks fail.
Output: Integration tests, updated documentation, and a manual verification checkpoint.
</objective>

<execution_context>
@D:/New-vp-main/.gemini/get-shit-done/workflows/execute-plan.md
@D:/New-vp-main/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-linkedin-auth-resiliency/08-02-SUMMARY.md
@src/lib/linkedin/session.ts
@TROUBLESHOOTING.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auth Integration Verification</name>
  <files>tests/auth.spec.ts</files>
  <behavior>
    - Test 1: Verify that `JSESSIONID` cookie is correctly parsed and injected.
    - Test 2: Verify that `storageState` JSON format (with `cookies` array) is correctly handled.
  </behavior>
  <action>
    Create `tests/auth.spec.ts` to test the injection logic.
    - Use a sample `storageState` JSON and verify that `injectFullStorageState` correctly calls `context.addCookies` with all expected properties.
  </action>
  <verify>
    <automated>npx playwright test tests/auth.spec.ts</automated>
  </verify>
  <done>Session injection logic verified for multi-cookie support including JSESSIONID.</done>
</task>

<task type="auto">
  <name>Task 2: Update Troubleshooting Documentation</name>
  <files>TROUBLESHOOTING.md</files>
  <action>
    Update `TROUBLESHOOTING.md` with a new section: "LinkedIn Session Resiliency (2025)".
    - Explain that `li_at` alone is no longer sufficient.
    - Provide step-by-step instructions for extracting a full Playwright `storageState.json`.
    - Mention the 'CHALLENGED' state and how to resolve it by manually solving a captcha in a headed browser.
    - Explain how to update the `LI_SESSION` environment variable with the full JSON string.
  </action>
  <verify>
    <automated>grep "LinkedIn Session Resiliency" TROUBLESHOOTING.md</automated>
  </verify>
  <done>Documentation updated to guide users through the new resilient session process.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full resilient LinkedIn automation stack with session health checks and role-based locators.</what-built>
  <how-to-verify>
    1. Extract a full storageState JSON from your browser (using an extension like 'EditThisCookie' or 'Cookie-Editor' exported as JSON, or using a Playwright script).
    2. Set `LI_SESSION` in `.env.local` to this JSON string.
    3. Run a test outreach request: `curl -X POST http://localhost:3000/api/linkedin/outreach -H "X-Outreach-Secret: your-secret" -d '{"profileUrl": "https://www.linkedin.com/in/some-profile", "message": "Resiliency Test"}'`
    4. Verify in logs/console that "Health Check: LOGGED_IN" is reported and the connection request is attempted.
  </how-to-verify>
  <resume-signal>Type "approved" if the health check and connection flow work as expected.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Documentation -> User | Guidance on handling sensitive session data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information Disclosure | `TROUBLESHOOTING.md` | mitigate | Add a clear security warning to the documentation advising users NEVER to share their session JSON with others. |
</threat_model>

<verification>
Ensure integration tests pass and documentation is clear and accurate.
</verification>

<success_criteria>
- `tests/auth.spec.ts` passes.
- `TROUBLESHOOTING.md` updated with the new section.
- Manual verification confirms end-to-end functionality.
</success_criteria>

<output>
After completion, create `.planning/phases/08-linkedin-auth-resiliency/08-03-SUMMARY.md`
</output>
