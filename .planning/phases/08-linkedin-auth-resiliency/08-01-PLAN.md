---
phase: 08-linkedin-auth-resiliency
plan: 01
type: complete
wave: 1
depends_on: []
files_modified: [src/lib/linkedin/session.ts, src/lib/linkedin/selectors.ts, tests/health.spec.ts]
autonomous: true
requirements: [LNK-08-01, LNK-08-02, LNK-08-03]
user_setup: []

must_haves:
  truths:
    - "LinkedIn session can be injected from a full storageState JSON"
    - "Session health can be verified as LOGGED_IN, LOGGED_OUT, or CHALLENGED"
    - "UI selectors are centralized and use ARIA roles/labels"
  artifacts:
    - path: "src/lib/linkedin/session.ts"
      provides: "Session injection and health check logic"
    - path: "src/lib/linkedin/selectors.ts"
      provides: "Resilient ARIA-based selectors"
    - path: "tests/health.spec.ts"
      provides: "Mocked verification of health check states"
  key_links:
    - from: "src/lib/linkedin/session.ts"
      to: "playwright-core (BrowserContext)"
      via: "addCookies/addInitScript"
---

<objective>
Establish the foundation for resilient LinkedIn automation by implementing full storage state injection, a robust health check utility, and a centralized repository of ARIA-based UI selectors.

Purpose: Move away from fragile single-cookie injection and CSS-heavy selectors that break when LinkedIn updates its DOM.
Output: Core session and selector utilities with mocked test verification.
</objective>

<execution_context>
@D:/New-vp-main/.gemini/get-shit-done/workflows/execute-plan.md
@D:/New-vp-main/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-linkedin-auth-resiliency/08-RESEARCH.md
@.planning/phases/08-linkedin-auth-resiliency/08-PATTERNS.md
@src/lib/browser.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement LinkedIn Session & Health Utility</name>
  <files>src/lib/linkedin/session.ts</files>
  <action>
    Create `src/lib/linkedin/session.ts` following the **Full Storage State Injection** pattern from 08-PATTERNS.md and research in 08-RESEARCH.md.
    - Implement `injectFullStorageState(context, sessionJson)`: Parse full Playwright storageState JSON and inject cookies via `context.addCookies`. 
    - Note: For localStorage, use `page.addInitScript` if needed, but prioritize cookies (li_at, JSESSIONID) as per D-01.
    - Implement `checkLoginHealth(page)`: Returns 'LOGGED_IN' | 'LOGGED_OUT' | 'CHALLENGED'.
    - Use `page.getByRole('link', { name: 'Home' })` to verify login on `linkedin.com/feed/`.
    - Detect 'CHALLENGED' by checking for 'Security Check' text or captcha locators.
  </action>
  <verify>
    <automated>npx tsc src/lib/linkedin/session.ts --noEmit</automated>
  </verify>
  <done>Session utility implemented with injection and health check logic.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize Resilient Selectors</name>
  <files>src/lib/linkedin/selectors.ts</files>
  <action>
    Create `src/lib/linkedin/selectors.ts` to centralize UI locators.
    - Use the ARIA role and label patterns from 08-PATTERNS.md.
    - Include selectors for: Profile Name, Connect button (primary and More menu), Add Note button, Message textbox, and Send button.
    - Avoid CSS classes like `.artdeco-button` unless no ARIA alternative exists.
  </action>
  <verify>
    <automated>ls src/lib/linkedin/selectors.ts</automated>
  </verify>
  <done>Centralized selector map created using resilient locator strategies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mock Health Check Verification</name>
  <files>tests/health.spec.ts</files>
  <behavior>
    - Test 1: Returns 'LOGGED_IN' when 'Home' link is visible.
    - Test 2: Returns 'LOGGED_OUT' when redirected to '/login' or '/authwall'.
    - Test 3: Returns 'CHALLENGED' when 'Security Check' text appears.
  </behavior>
  <action>
    Create `tests/health.spec.ts` using Playwright's test runner.
    - Use `page.route` to mock LinkedIn responses for the three states.
    - Verify that `checkLoginHealth` from `src/lib/linkedin/session.ts` returns the correct status for each mock.
  </action>
  <verify>
    <automated>npx playwright test tests/health.spec.ts</automated>
  </verify>
  <done>Health check logic verified against mocked LinkedIn states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> API | Raw session JSON provided by user is injected into browser |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | `session.ts` | mitigate | Ensure session JSON is never logged to console or stored in unencrypted local logs. |
| T-08-02 | Denial of Service | `session.ts` | mitigate | Implement strict timeouts (30s) for health checks to prevent hanging processes. |
| T-08-03 | Tampering | `actions.ts` | mitigate | Ensure `note` and `message` strings are trimmed and length-limited before injection. |
</threat_model>

<verification>
Validate that `session.ts` correctly identifies a logged-in state vs a challenged state using the mock tests.
</verification>

<success_criteria>
- `src/lib/linkedin/session.ts` exports `injectFullStorageState` and `checkLoginHealth`.
- `src/lib/linkedin/selectors.ts` exports `SELECTORS`.
- `npx playwright test tests/health.spec.ts` passes all 3 behavior cases.
</success_criteria>

<output>
After completion, create `.planning/phases/08-linkedin-auth-resiliency/08-01-SUMMARY.md`
</output>
