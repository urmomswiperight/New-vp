---
phase: 08-linkedin-auth-resiliency
plan: 02
type: complete
wave: 2
depends_on: [08-01]
files_modified: [src/lib/linkedin/actions.ts, src/app/api/linkedin/outreach/route.ts, src/lib/linkedin/outreach.ts]
autonomous: true
requirements: [LNK-08-02, LNK-08-03]
user_setup: []

must_haves:
  truths:
    - "Outreach actions use centralized resilient selectors"
    - "API route performs a health check before executing browser actions"
    - "Human-like typing delay is applied to all messages"
  artifacts:
    - path: "src/lib/linkedin/actions.ts"
      provides: "High-level resilient automation actions (Connect, Message)"
    - path: "src/app/api/linkedin/outreach/route.ts"
      provides: "Refactored entry point with health guards"
  key_links:
    - from: "src/lib/linkedin/actions.ts"
      to: "src/lib/linkedin/selectors.ts"
      via: "import { SELECTORS }"
    - from: "src/app/api/linkedin/outreach/route.ts"
      to: "src/lib/linkedin/session.ts"
      via: "checkLoginHealth(page)"
---

<objective>
Migrate existing LinkedIn automation workflows to use the new resilient session management and role-based selector logic.

Purpose: Upgrade the implementation to survive LinkedIn UI updates and provide better error feedback when sessions fail.
Output: Refactored actions library and API route with proactive health checks.
</objective>

<execution_context>
@D:/New-vp-main/.gemini/get-shit-done/workflows/execute-plan.md
@D:/New-vp-main/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-linkedin-auth-resiliency/08-01-SUMMARY.md
@src/lib/linkedin/selectors.ts
@src/lib/linkedin/session.ts
@src/lib/linkedin/outreach.ts
@src/app/api/linkedin/outreach/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Resilient Automation Actions</name>
  <files>src/lib/linkedin/actions.ts</files>
  <action>
    Create `src/lib/linkedin/actions.ts` refactoring logic from `outreach.ts`.
    - Implement `sendConnectionRequest(page, note)`: Use `SELECTORS.profile.connect`. Handle the "More" menu fallback using `SELECTORS.profile.more`.
    - Implement `sendMessage(page, message)`: Use `SELECTORS.profile.messageTextbox`.
    - Apply human-like typing: `await page.keyboard.type(char, { delay: 40 + Math.random() * 60 })`.
    - Ensure all actions use `getByRole` or `getByLabel` via the centralized `SELECTORS`.
  </action>
  <verify>
    <automated>npx tsc src/lib/linkedin/actions.ts --noEmit</automated>
  </verify>
  <done>Modular resilient actions implemented and decoupled from main outreach script.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Outreach API Route with Health Guards</name>
  <files>src/app/api/linkedin/outreach/route.ts</files>
  <action>
    Update `src/app/api/linkedin/outreach/route.ts` to use the new modular logic.
    - Replace single cookie injection with `injectFullStorageState` from `session.ts`.
    - Mandatory Health Check: Call `checkLoginHealth(page)` before navigating to profile.
    - If health is 'LOGGED_OUT' or 'CHALLENGED', return a descriptive 401/403 error immediately instead of failing on navigation.
    - Integrate the refactored actions from `actions.ts`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>API route upgraded with session injection and proactive health verification.</done>
</task>

<task type="auto">
  <name>Task 3: Update Legacy Outreach Utility</name>
  <files>src/lib/linkedin/outreach.ts</files>
  <action>
    Refactor `src/lib/linkedin/outreach.ts` to use `src/lib/linkedin/session.ts` and `src/lib/linkedin/actions.ts`.
    - Clean up the monolithic `runLinkedInOutreach` function.
    - Delegate browser launch and auth to the new utilities.
    - Replace hardcoded CSS selectors with `SELECTORS` from `selectors.ts`.
    - Maintain existing daily limit and screenshot logic.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Legacy utility refactored to use the new resilient architecture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API -> LinkedIn UI | Automated interactions with the LinkedIn DOM |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | `actions.ts` | mitigate | Ensure `note` and `message` strings are trimmed and length-limited before injection. |
</threat_model>

<verification>
Ensure the project builds successfully and the API route logic correctly handles session health status.
</verification>

<success_criteria>
- `src/lib/linkedin/actions.ts` implements connection and messaging logic.
- `src/app/api/linkedin/outreach/route.ts` rejects requests if `checkLoginHealth` fails.
- Project compiles without type errors.
</success_criteria>

<output>
After completion, create `.planning/phases/08-linkedin-auth-resiliency/08-02-SUMMARY.md`
</output>
