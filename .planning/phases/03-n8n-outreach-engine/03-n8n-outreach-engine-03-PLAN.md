---
phase: 03-n8n-outreach-engine
plan: 03
type: execute
wave: 2
depends_on: ["03-n8n-outreach-engine-01"]
files_modified: ["n8n_linkedin_workflow.json", "scripts/linkedin-outreach.ts"]
autonomous: true
requirements: [LINKEDIN-WF]
user_setup:
  - service: Playwright
    why: "Local LinkedIn automation library"
    command: "npm install playwright"
  - task: "Ensure LinkedIn session is active in the local browser or provide credentials"
    location: "Local Machine"

must_haves:
  truths:
    - "n8n triggers LinkedIn actions via local Playwright script"
    - "Rate limits are respected (max 10-20/day)"
    - "LinkedIn outreach status is synced to Supabase"
  artifacts:
    - path: "n8n_linkedin_workflow.json"
      provides: "LinkedIn automation integration"
    - path: "scripts/linkedin-outreach.ts"
      provides: "Playwright script for LinkedIn interactions"
  key_links:
    - from: "n8n Workflow"
      to: "Local Script"
      via: "Execute Command node"
      pattern: "npx ts-node scripts/linkedin-outreach.ts"
---

<objective>
Develop the LinkedIn outreach logic using local Playwright automation, ensuring safe interactions and seamless status syncing with the dashboard.

Purpose: Automates LinkedIn interactions locally to maintain a $0 stack and full control over the automation process.
Output: An n8n workflow and a supporting Playwright script.
</objective>

<execution_context>
@C:/Users/mikiy/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/mikiy/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-n8n-outreach-engine/RESEARCH.md
@.planning/phases/03-n8n-outreach-engine/03-n8n-outreach-engine-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Develop Local LinkedIn Script</name>
  <files>scripts/linkedin-outreach.ts</files>
  <action>
    Create a Playwright script `scripts/linkedin-outreach.ts` that:
    1. Accepts arguments for LinkedIn profile URL and message.
    2. Uses a local browser profile (to avoid repeated logins and detection).
    3. Navigates to the profile, clicks "Connect" (if available), and adds the personalized message.
    4. Handles basic edge cases (already connected, message already sent, "Follow" instead of "Connect").
    5. Returns a JSON success/failure status.
  </action>
  <verify>
    Run the script manually from the CLI with a test URL and message to ensure it navigates and interacts correctly.
  </verify>
  <done>A functional Playwright script for LinkedIn outreach is available locally.</done>
</task>

<task type="auto">
  <name>Task 2: n8n LinkedIn Workflow Integration</name>
  <files>n8n_linkedin_workflow.json</files>
  <action>
    Create or update the n8n workflow:
    1. Add an "Execute Command" node to call the Playwright script using `npx ts-node scripts/linkedin-outreach.ts`.
    2. Pass the lead's LinkedIn URL and message (generated by the Ollama node) as arguments.
    3. Implement a limit check (using a Counter or Supabase query) to ensure no more than 10-20 LinkedIn actions are performed per day.
    4. Add a Supabase node to update the Lead record: set `status` to 'Contacted (LinkedIn)' after a successful execution.
    5. Export the workflow JSON.
  </action>
  <verify>
    Trigger the n8n workflow for a test lead and confirm the script runs and the status is updated in Supabase.
  </verify>
  <done>n8n is integrated with the local LinkedIn script and maintains status tracking.</done>
</task>

</tasks>

<verification>
Ensure LinkedIn automation is correctly triggered by n8n, respects daily limits, and updates lead statuses in Supabase.
</verification>

<success_criteria>
- Playwright script interacts with LinkedIn correctly.
- n8n "Execute Command" node triggers the script successfully.
- Daily action limits are enforced.
- Leads table reflects 'Contacted (LinkedIn)'.
</success_criteria>

<output>
After completion, create `.planning/phases/03-n8n-outreach-engine/03-n8n-outreach-engine-03-SUMMARY.md`
</output>
