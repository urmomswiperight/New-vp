---
phase: 04-ai-onboarding-research
plan: 01
type: execute
wave: 1
depends_on: ["03-n8n-outreach-engine-01"]
files_modified: ["n8n_research_workflow.json", "src/app/dashboard/research/page.tsx", "src/app/api/research/summarize/route.ts"]
autonomous: true
requirements: [AI-SUMMARIZE, RESEARCH-PAGE]
user_setup:
  - service: Ollama
    why: "Local AI engine for summarization"
    command: "ollama run deepseek-v2"

must_haves:
  truths:
    - "n8n receives transcripts via webhook and returns an AI summary"
    - "Summaries are generated by local Ollama at http://localhost:11434"
    - "Dashboard allows transcript upload and displays the generated summary"
  artifacts:
    - path: "n8n_research_workflow.json"
      provides: "n8n workflow for transcript processing"
    - path: "src/app/dashboard/research/page.tsx"
      provides: "UI for transcript upload and report viewing"
  key_links:
    - from: "Next.js"
      to: "n8n Webhook"
      via: "POST /api/research/summarize"
    - from: "n8n"
      to: "Ollama"
      via: "HTTP Request to localhost:11434"
---

<objective>
Develop a local-first AI meeting summarizer that processes meeting transcripts through n8n and Ollama, displaying results in a new dashboard research view.

Purpose: Automates the transition from discovery calls to strategic action plans.
Output: An n8n workflow for summarization and a functional dashboard research page.
</objective>

<execution_context>
@C:/Users/mikiy/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/mikiy/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-onboarding-research/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: n8n Meeting Summarizer Workflow</name>
  <files>n8n_research_workflow.json</files>
  <action>
    Create a new n8n workflow `n8n_research_workflow.json`:
    1. **Webhook Trigger**: Receives `transcript` (text) and `leadId` (optional).
    2. **Ollama HTTP Node**: Sends the transcript to `http://localhost:11434/api/chat` with a prompt:
       - "Summarize this meeting transcript: [transcript]. Extract: 1. Main Goal, 2. Top 3 Pain Points, 3. Next Actions."
       - Use model `deepseek-v2` or `llama3`.
    3. **Supabase Update**: (Optional) Store the summary in a `Research` or `Lead` table.
    4. **Return Result**: Send the JSON summary back as the webhook response.
  </action>
  <verify>
    Test the workflow with a mock transcript in n8n and verify the AI summary is generated correctly.
  </verify>
  <done>n8n can process transcripts through local Ollama.</done>
</task>

<task type="auto">
  <name>Task 2: Research Dashboard UI</name>
  <files>src/app/dashboard/research/page.tsx, src/app/api/research/summarize/route.ts</files>
  <action>
    1. Create `src/app/dashboard/research/page.tsx`:
       - Add a text area or file upload for meeting transcripts.
       - Implement a "Generate Summary" button.
       - Display the resulting AI summary in a card layout.
    2. Create `src/app/api/research/summarize/route.ts`:
       - Authenticate user.
       - Trigger the n8n research webhook.
       - Return the summary to the frontend.
  </action>
  <verify>
    Upload a transcript from the UI and confirm the AI summary appears on the page.
  </verify>
  <done>Users can upload transcripts and view AI-generated summaries in the dashboard.</done>
</task>

</tasks>

<verification>
Verify end-to-end: Upload transcript -> n8n processes via Ollama -> UI displays structured summary.
</verification>

<success_criteria>
- n8n workflow uses local Ollama.
- Dashboard research page is functional.
- Summaries correctly extract goals, pain points, and actions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-onboarding-research/04-ai-onboarding-research-01-SUMMARY.md`
</output>
