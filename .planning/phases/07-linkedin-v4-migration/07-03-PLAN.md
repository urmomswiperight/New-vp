---
phase: 07-linkedin-v4-migration
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: [n8n_linkedin_api_v4.json]
autonomous: false
requirements: [LNK-03, LNK-05]
must_haves:
  truths:
    - "n8n workflow file is created and configured for the API"
    - "Local verification shows the API responds correctly to valid and invalid requests"
  artifacts:
    - path: "n8n_linkedin_api_v4.json"
      provides: "Cloud-ready n8n workflow for LinkedIn automation"
  key_links:
    - from: "n8n workflow"
      to: "/api/linkedin/outreach"
      via: "HTTP POST request"
---

<objective>
Finalize the n8n integration and verify the end-to-end functionality of the LinkedIn outreach API.

Purpose: To ensure the system is ready for use by n8n Cloud and that the API is properly secured and functional.
Output: Cloud-ready n8n workflow and verified API.
</objective>

<execution_context>
@C:/Users/mikiy/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/mikiy/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-linkedin-v4-migration/07-02-SUMMARY.md
@src/app/api/linkedin/outreach/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create n8n LinkedIn API v4 Workflow</name>
  <files>n8n_linkedin_api_v4.json</files>
  <action>
    Create a new n8n workflow file `n8n_linkedin_api_v4.json`. 
    
    The workflow should:
    1. **Trigger**: Manual trigger (for testing) or an appropriate trigger node.
    2. **HTTP Request Node**: Configure to call `POST http://localhost:3000/api/linkedin/outreach` (use a variable for the base URL if possible).
       - Method: POST
       - Header: `X-Outreach-Secret` (using a variable for the secret).
       - Body: JSON with `profileUrl` and `message` from previous nodes or static test data.
    3. **Success/Error Handling**: Include nodes to process the JSON response from the API.
    
    This workflow replaces the local script execution with a cloud-ready API call.
  </action>
  <verify>Check the JSON structure for the HTTP Request node configuration.</verify>
  <done>n8n_linkedin_api_v4.json is created with correct API call configuration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>LinkedIn Outreach API and n8n Workflow integration.</what-built>
  <how-to-verify>
    1. Ensure the Next.js app is running locally (npm run dev).
    2. Use `curl` to test the API route:
       ```bash
       curl -X POST http://localhost:3000/api/linkedin/outreach \
            -H "Content-Type: application/json" \
            -H "X-Outreach-Secret: YOUR_SECRET" \
            -d '{"profileUrl": "https://www.linkedin.com/in/test-profile/", "message": "SCROLL_AND_SCRAPE"}'
       ```
    3. Verify that the response is structured as expected (even if it fails auth on LinkedIn side, the API logic should trigger).
    4. Import the n8n workflow into a local n8n instance and run it to verify it correctly communicates with the Next.js API.
    5. Check the logs/console of the Next.js app for debug output.
  </how-to-verify>
  <resume-signal>Type "approved" once local verification is complete.</resume-signal>
</task>

</tasks>

<verification>
Verify that the n8n workflow file exists and contains the correct API endpoint URL.
Perform the manual verification steps to confirm the API is reachable and responds correctly.
</verification>

<success_criteria>
n8n workflow is ready for cloud use, and the API is verified to be functioning and secured.
</success_criteria>

<output>
After completion, create `.planning/phases/07-linkedin-v4-migration/07-03-SUMMARY.md`
</output>
