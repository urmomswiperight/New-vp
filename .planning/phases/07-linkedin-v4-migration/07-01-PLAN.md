---
phase: 07-linkedin-v4-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/linkedin/outreach.ts, scripts/linkedin-outreach-v2.ts, .env.local, .env.example]
autonomous: true
requirements: [LNK-02, LNK-04]
must_haves:
  truths:
    - "Core LinkedIn logic is extracted to a reusable library"
    - "Outreach script is updated to use the library"
    - "Outreach secret is configured for the environment"
  artifacts:
    - path: "src/lib/linkedin/outreach.ts"
      provides: "Refactored core outreach logic with stealth and limit checks"
    - path: "scripts/linkedin-outreach-v2.ts"
      provides: "Lightweight CLI wrapper for the outreach logic"
    - path: ".env.local"
      provides: "OUTREACH_SECRET for API authentication"
  key_links:
    - from: "scripts/linkedin-outreach-v2.ts"
      to: "src/lib/linkedin/outreach.ts"
      via: "import and call function"
---

<objective>
Refactor the LinkedIn outreach logic into a shared library with enhanced stealth measures and set up the necessary environment secrets.

Purpose: To provide a robust, reusable core for both the CLI script and the upcoming API route, ensuring consistent behavior and safety.
Output: Shared logic library, updated script, and configured environment variables.
</objective>

<execution_context>
@C:/Users/mikiy/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/mikiy/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/linkedin-outreach-v2.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Outreach Secret</name>
  <files>.env.local, .env.example</files>
  <action>
    Add a secure random string for `OUTREACH_SECRET` to `.env.local`. Create or update `.env.example` to include the key without the value. This secret will be used to authenticate requests to the LinkedIn API route.
  </action>
  <verify>Check .env.local for OUTREACH_SECRET.</verify>
  <done>OUTREACH_SECRET exists in .env.local and .env.example.</done>
</task>

<task type="auto">
  <name>Task 2: Create Shared LinkedIn Outreach Library</name>
  <files>src/lib/linkedin/outreach.ts</files>
  <action>
    Extract the core logic from `scripts/linkedin-outreach-v2.ts` into a `runLinkedInOutreach` function in `src/lib/linkedin/outreach.ts`. 
    
    Enhancements:
    - **Stealth**: Implement User-Agent rotation (using common Chrome, Firefox, Safari UAs).
    - **Jitter**: Increase randomized delays for navigation and typing actions to be more human-like.
    - **Limits**: Move daily limit checking into the core function. Ensure it throws or returns a specific status if the limit is reached.
    - **Session**: Ensure Playwright `userDataDir` is correctly managed (pointing to `.playwright-sessions`).
    - **Error Handling**: Use structured error responses.
  </action>
  <verify>Code check for stealth logic (UA rotation, jitter) and daily limit enforcement.</verify>
  <done>src/lib/linkedin/outreach.ts is created with the enhanced logic.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Outreach Script to Wrapper</name>
  <files>scripts/linkedin-outreach-v2.ts</files>
  <action>
    Modify `scripts/linkedin-outreach-v2.ts` to:
    1. Import `runLinkedInOutreach` from `../src/lib/linkedin/outreach`.
    2. Pass command line arguments (`process.argv[2]`, `process.argv[3]`, etc.) to the function.
    3. Handle the output/logs accordingly.
    
    This ensures the script continues to work but uses the shared logic.
  </action>
  <verify>Run `npx ts-node scripts/linkedin-outreach-v2.ts` without arguments to ensure it still prints missing arguments error correctly (via shared logic or wrapper check).</verify>
  <done>The script is successfully refactored to use the shared library.</done>
</task>

</tasks>

<verification>
Check for existence of `src/lib/linkedin/outreach.ts` and verify it contains the `runLinkedInOutreach` function with stealth/limit features.
Confirm `.env.local` contains `OUTREACH_SECRET`.
</verification>

<success_criteria>
Shared logic library exists, script is updated, and secrets are set.
</success_criteria>

<output>
After completion, create `.planning/phases/07-linkedin-v4-migration/07-01-SUMMARY.md`
</output>
