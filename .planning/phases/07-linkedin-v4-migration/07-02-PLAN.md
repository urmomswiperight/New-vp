---
phase: 07-linkedin-v4-migration
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: [src/app/api/linkedin/outreach/route.ts]
autonomous: true
requirements: [LNK-01]
must_haves:
  truths:
    - "API route exists and authenticates via secret header"
    - "API route successfully invokes the shared outreach logic"
    - "API returns structured JSON response (success/error/logs)"
  artifacts:
    - path: "src/app/api/linkedin/outreach/route.ts"
      provides: "Secure API endpoint for LinkedIn automation"
  key_links:
    - from: "src/app/api/linkedin/outreach/route.ts"
      to: "src/lib/linkedin/outreach.ts"
      via: "import and call runLinkedInOutreach"
---

<objective>
Implement the LinkedIn outreach API wrapper to expose the Playwright logic as a secure endpoint.

Purpose: To allow remote systems like n8n Cloud to trigger LinkedIn automation without local execution privileges.
Output: Secure Next.js API route.
</objective>

<execution_context>
@C:/Users/mikiy/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/mikiy/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-linkedin-v4-migration/07-01-SUMMARY.md
@src/lib/linkedin/outreach.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LinkedIn Outreach API Route</name>
  <files>src/app/api/linkedin/outreach/route.ts</files>
  <action>
    Create a POST endpoint at `src/app/api/linkedin/outreach/route.ts`. 
    
    Implementation details:
    1. **Authentication**: Verify the `X-Outreach-Secret` header matches `process.env.OUTREACH_SECRET`. Return 401 if it doesn't.
    2. **Request Body**: Parse `profileUrl` (required), `message` (required), and `dailyLimit` (optional, default to 25). Validate these fields and return 400 if missing or invalid.
    3. **Execution**: Import `runLinkedInOutreach` from `@/lib/linkedin/outreach` and await its execution. 
    4. **Response**: 
       - Return 200 with the result of the outreach if successful (e.g., `{ success: true, status: 'Sent', countToday: 5 }`).
       - Return 500 or appropriate error code with error details if it fails.
    5. **Environment**: Ensure it uses the Node.js runtime (default).
  </action>
  <verify>Check the file structure and content to ensure correct header verification and logic invocation.</verify>
  <done>src/app/api/linkedin/outreach/route.ts is fully implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement API Logging</name>
  <files>src/app/api/linkedin/outreach/route.ts</files>
  <action>
    Add console logging to the API route to capture start/end of outreach attempts, including the profile being targeted (masking sensitive parts if necessary) and the outcome. This helps in debugging when the API is called remotely.
  </action>
  <verify>Check for console.log statements in the route implementation.</verify>
  <done>Logging is integrated into the API route.</done>
</task>

</tasks>

<verification>
Verify that the API route correctly imports the shared logic.
Verify that the `X-Outreach-Secret` header is checked.
</verification>

<success_criteria>
Next.js API route exists and correctly wraps the LinkedIn outreach logic with authentication.
</success_criteria>

<output>
After completion, create `.planning/phases/07-linkedin-v4-migration/07-02-SUMMARY.md`
</output>
