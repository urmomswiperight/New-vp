app = "playwright-runner"
primary_region = "iad"

[build]
  dockerfile = "docker/playwright-runner.Dockerfile"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  size = "shared-cpu-1x"
  memory = "1gb"
